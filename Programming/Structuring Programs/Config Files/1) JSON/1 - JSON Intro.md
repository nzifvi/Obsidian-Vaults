[[2 - JSON Types]] <- Next

---
## JSON Intro
JSON is a file format, typically used for config files/APIs.
- Used because lightweight (small file size) and easy to read/write.
- Also used because it can be used with many programming languages.
	- Some languages have the functionality built in but others require a library.