[[4) Restrictions of Generics]] <- Back
[[2,1) Arrays]] <- Next

---
## Variables

Variables hold a value, which is a primitive data type (int, char, float, double, etc) OR a pointer to an object.

Item must have the data type declared for it.

Variables hold a single value / object.

