[[3) Wild Cards]] <- Back
[[1,1) Grouping Data]] <- Next

---
1) Cannot specify a type parameter to be a primitive data type.
	- Can get around this using auto-boxing.

2) Cannot create an instance of a type parameter.

3) Cannot have a type parameter be the data type of a static variable/method.
	- These must be known during runtime.

4) Cannot use casts, or instanceof, with parameterised types.

5) Cannot 