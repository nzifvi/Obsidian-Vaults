[[2) Garbage Collection]] <- Back
[[4) Heap Space]] <- Next

---
## Garbage Collection in Java
In Java, garbage collection happens automatically.
- It occurs when the Heap becomes full.
- Java Garbage Collection is much less error prone than programming garbage collection process manually in languages without an automatic garbage collector.
- However, programmer can also call System.gc() to request a garbage collection.