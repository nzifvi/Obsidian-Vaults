[[9,1 - Mutation]] $\leftarrow$ Back
[[9,3 - Gaussian Mutation]] $\leftarrow$ Next

---
# What is Standard Mutation?
Standard mutation is the approach to mutation where each gene, in a genotype, has an equivalent probability to mutate.