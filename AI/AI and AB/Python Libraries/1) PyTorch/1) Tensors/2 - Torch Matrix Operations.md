[[1 - About PyTorch Tensors]] $\leftarrow$ Back

---
# 1) torch.mm()
`torch.mm(input, mat2)` is used to multiply an input matrix by mat2.
$$
out = input\cdot mat2
$$
- Inner dimensions must match.

# 2) T
Any given tensor can be transposed via .T operator appended to end of a given Tensor.