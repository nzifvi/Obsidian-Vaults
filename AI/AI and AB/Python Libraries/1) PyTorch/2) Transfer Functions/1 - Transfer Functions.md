[[2 - Derivatives of Transfer Functions]] $\leftarrow$ Next

---
PyTorch library has multiple transfer functions which take a singular input and return a value.
- Used for NNs during forward passes.

