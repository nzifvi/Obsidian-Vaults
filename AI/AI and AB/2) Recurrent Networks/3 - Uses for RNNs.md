[[2,2 - Cyclic Behaviour of RNNs]] $\leftarrow$ Back
[[4,1 - Training RNNs]] $\leftarrow$ Next

---
# Uses for RNNs
RNNs are useful for short-term memory.
- Dynamics of RNNs allow for a form of memory.
- Allows for process of sequence of inputs, like language, which allows for implementations of models such as LLMs.
