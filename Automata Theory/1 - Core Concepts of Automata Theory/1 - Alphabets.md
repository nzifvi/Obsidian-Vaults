[[2 - Strings]] <- Next

---
## What are Alphabets
An alphabet, $\sum$, is a set of symbols.

$$
\sum = \{a,b,c\}
$$

The cardinality, aka size, of an alphabet is the number of symbols it has.

If $\sum$ = {a, b, c}, then $|\sum|$ = 3

Alphabets form the foundation of strings and languages.