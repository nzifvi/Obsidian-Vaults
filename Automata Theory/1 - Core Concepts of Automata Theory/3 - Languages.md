[[2 - Strings]] <- Back

---
## What are Languages?
Languages can be defined as the set of all possible strings of infinite length of a given alphabet.
- $\sum.^*$ is a language over alphabet $\sum$

This operation can be expressed as...
$$
\sum.^* = 
$$

Suppose $\sum = \{a,b,c\}$
