[[2) __str__() function member]] <- Back
[[4) Inheritance]] <- Next

---
## What is `self`
self is a reserve word used by a class to reference itself, or it's data members, throughout a class.

Can be used to...
- Define a function member which has a parameter of the object itself.
- Use it like this reserve word in C/C++