[[1) Python Comments]] <- Back

---
## Python Indentation

Python does not use curly braces to create blocks of code, it uses indentation.
- In other languages, indentation is not required. It just improves readability.

In Python, each indent denotes a block of code.

```python
if 5 > 2:
	print("greater")
```