[[2 - Template Classes and Functions]] <- Next

---
## What are Templates?
A template is a mechanism to use datatypes as parameters for a class or a function.
- It allows the data type, of something, to be unknown and later declared when whatever uses the template type parameter is used.

The C++ compiler generates a specific class/function, of a template class/function when the datatype is specified as an argument.