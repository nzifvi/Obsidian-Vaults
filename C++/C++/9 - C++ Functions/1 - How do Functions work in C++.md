[[3,1 - Function Pointers]] <- Back

---
## How do Functions work in C++
Functions are also objects in memory, and have their own datatype (the function datatype) that identifies what that section of memory is.

When a function is called, using (), execution jumps to the memory address of the called function and begins execution.

