

---
# 1) Splitting a String into a list of Substrings
Strings have a built in function-member, `split()`, which returns a list of substrings, which are individual words in the substring.
```python
str = "hello i am"
print(str.split())
```
