[[1,1) Git Setup]] <- Back
[[1,3) Creating  a Repository]] <- Next

---
## Using Git via Terminal
Two options:

1) Can use Command Prompt, and use cd to choose the directory to init git inside of.

2) Sometimes, IDEs provide terminals. Can use Git there and the directory will automatically be the one the program is inside of.

