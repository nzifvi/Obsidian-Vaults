[[1,2) Using Git via Terminal]] <- Back
[[1,4) Tracking Files & Commits]] <- Next

---
## Creating a Repository
When a directory's file path is selected, or you are inside of a project in an IDE, a git repository can be made.

```git
git init
```
- creates a git repository for the directory currently selected.